# Ubuntu Setup Script

Script otomatis untuk instalasi paket setelah install ulang Ubuntu, termasuk optimasi untuk laptop AMD.

## Cara Pakai

Setelah install ulang Ubuntu, cukup jalankan:

```bash
bash <(curl -s https://raw.githubusercontent.com/kyribo/ubuntu-setup/main/install.sh)
